export type Product = {
  image: string;
  discount?: string;
  name: string;
  currentPrice: string;
  regularPrice: string;
  sold: number;
  rating: number;
};
export type Banner = {
  image: string;
  link: string;
  alt: string;
};
